// Product Management Dashboard

const productList = document.querySelector(".product-manage-table");
const deleteBtn = productList.querySelectorAll(".fa-trash");

// deleteBtn.forEach(btn => btn.onclick = () => )
